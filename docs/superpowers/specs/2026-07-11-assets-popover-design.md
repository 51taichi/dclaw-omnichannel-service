# 会话资产浮层设计

## 目标

客户会话中的资产面板展开时不再影响聊天布局，避免聊天记录和底部回复框被顶开。

## 设计

- 资产面板仍由当前资产图标触发。
- 面板改为聊天视图内的浮层，使用绝对定位覆盖在聊天内容上方。
- 面板靠近右上角展示，最大高度受限，内容过多时内部滚动。
- 切换会话或没有资产时继续自动关闭。
- 现有资产数据结构和服务端接口不变。

## 验收

- `.chat-view` 是浮层定位容器。
- `.assets-panel` 使用绝对定位，不参与正常布局。
- 展开资产面板不会改变聊天记录和人工/AI 回复框的位置。
