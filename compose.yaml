services:
  worktool-bot-service:
    build: .
    container_name: worktool-bot-service
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3010:3000"
    volumes:
      - ./data:/app/data
