services:
  worktool-bot-service:
    build: .
    container_name: worktool-bot-service
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:18765:8765"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
